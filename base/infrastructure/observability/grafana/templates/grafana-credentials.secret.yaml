{{- if .Values.grafana.createSecret }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.grafana.existingSecret | quote }}
stringData:
  username: "{{ .Values.grafana.username }}"
  password: "{{ .Values.grafana.password }}"
type: Opaque
{{- end }}
