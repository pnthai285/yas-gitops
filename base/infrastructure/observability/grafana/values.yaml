hostname: grafana.yasdevops.dev
grafana:
  existingSecret: grafana-admin-credentials
  createSecret: false
  username: admin
  # Password must be provided via SealedSecret named: grafana-admin-credentials
  password: ""
postgresql:
  url: postgresql:5432
  username: yasadminuser
  # Password must be provided via SealedSecret or operator-managed Postgres user secret
  password: ""
