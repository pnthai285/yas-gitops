{{- if .Values.postgresql.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgresql.existingSecret | quote }}
type: Opaque
data:
  username: {{ .Values.postgresql.username | b64enc }}
  password: {{ .Values.postgresql.password | b64enc }}
{{- end }}
